export const ColoredLine = ({ color }) => (
    <hr
        style={{
            borderColor:color,
            height: 0.1,
            width : '100%',
            marginTop: 50,
            marginBottom:40,
        }}
    />
);